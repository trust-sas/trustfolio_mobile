# Copy this file to .env and set the backend's base URL to switch every
# repository in src/data/*Repository.ts from mock data (AsyncStorage) to
# real API calls. Leave empty / delete .env to keep using mock data.
#
# Example: EXPO_PUBLIC_API_BASE_URL=https://api.trustfoliokids.com
EXPO_PUBLIC_API_BASE_URL=
